apiVersion: v1
kind: Service
metadata:
  name: shrinkr-api
spec:
  type: NodePort
  selector:
    app: shrinkr-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
